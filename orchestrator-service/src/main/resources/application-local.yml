spring:
  kafka:
    bootstrap-servers: localhost:9093
  data:
    redis:
      host: localhost

ai:
  enrichment:
    base-url: http://localhost:8000

ticket:
  service:
    base-url: http://localhost:8081

logging:
  level:
    com.ticketai: DEBUG
